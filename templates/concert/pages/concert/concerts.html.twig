{% extends 'concert/base.html.twig' %}
    {% block header %} {% include('concert/components/header.html.twig') with {'mastPhrase' : "Les concerts prévus ! 🎸" , 'subMastPhrase' : ""} %}
    {% endblock %}

{% block content %}

    <section style="min-height: 100vh">
        <div class="container">
            <div class="row align-items-center">
                  <table style="margin : 0 auto">
                      <thead>
                          <tr>
                              <th>Groupe <span class="fa fa-users"></span> </th>
                              <th>Tournée</th>
                              <th>Date</th>
                              <th>Actions</th>
                          </tr>
                      </thead>
                      <tbody>
                      {% for concert in concerts %}
                          <tr>
                              <td>{% for band in concert.band %}
                                  {{ band.name }}
                              {% endfor %}
                              </td>
                              <td>{{ concert.tournee }}</td>
                              <td>{{ concert.date | date('d M Y') }}</td>
                              <td><button class="btn-danger"> Supprimer</button> </td>
                          </tr>
                      {% endfor %}
                      </tbody>
                  </table>
                <a href="{{ path('concert_create') }}">Créer un concert</a>
            </div>
        </div>
    </section>

{% endblock %}
