{% extends 'concert/base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
    {% block header %} {% include('concert/components/header.html.twig') with {'mastPhrase' : "Créer un nouveau groupe ! 🎸" , 'subMastPhrase' : ""} %}
    {% endblock %}

{% block content %}

    <section>
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-6">
                    {{ form(form) }}
                </div>
            </div>
        </div>
    </section>

{% endblock %}
